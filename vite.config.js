// ============================================
// ملف إعدادات Vite - أداة بناء المشروع
// ============================================
// Vite هو أداة بناء سريعة لمشاريع الويب الحديثة
// هذا الملف يحدد كيف يتم بناء وتشغيل المشروع

// استيراد دالة defineConfig من مكتبة Vite لتعريف الإعدادات
import { defineConfig } from 'vite'
// استيراد إضافة React التي تسمح لـ Vite بفهم ملفات JSX
import react from '@vitejs/plugin-react'

// تصدير الإعدادات الافتراضية للمشروع
export default defineConfig({
  // الإضافات المستخدمة - هنا نستخدم إضافة React
  plugins: [react()],

  // المسار الأساسي للتطبيق عند النشر على GitHub Pages
  // هذا يعني أن التطبيق سيعمل على: username.github.io/Sec-Ops-version-1/
  base: '/Sec-Ops/',

  // إعدادات خادم التطوير المحلي
  server: {
    // رقم المنفذ الذي سيعمل عليه التطبيق محلياً (localhost:3000)
    port: 3000,
    // فتح المتصفح تلقائياً عند تشغيل الخادم
    open: true
  }
})
