// ============================================
// ملف إعدادات Tailwind CSS - مكتبة التنسيق
// ============================================
// Tailwind CSS هي مكتبة CSS تسمح لك بتصميم الواجهات باستخدام أصناف (classes) جاهزة
// هذا الملف يخصص الألوان والخطوط والحركات المستخدمة في التطبيق

/** @type {import('tailwindcss').Config} */
export default {
  // تحديد الملفات التي يبحث فيها Tailwind عن الأصناف المستخدمة
  // هذا يساعد على حذف الأصناف غير المستخدمة من الملف النهائي لتقليل حجمه
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}", // كل ملفات JavaScript و TypeScript في مجلد src
  ],

  // تفعيل الوضع الداكن باستخدام صنف 'dark' على عنصر HTML
  darkMode: 'class',

  // تخصيص وتوسيع الثيم الافتراضي
  theme: {
    extend: {
      // ============ الألوان المخصصة ============
      colors: {
        // لوحة ألوان الوضع الداكن عالية التباين
        // navy = اللون الأزرق الداكن (البحري) - يُستخدم كخلفية رئيسية
        navy: {
          50: '#f0f4f8',   // أفتح درجة - تقريباً أبيض مع لمسة زرقاء
          100: '#d9e2ec',
          200: '#bcccdc',
          300: '#9fb3c8',
          400: '#829ab1',
          500: '#627d98',  // درجة متوسطة
          600: '#486581',
          700: '#334e68',
          800: '#243b53',
          900: '#102a43',
          950: '#020617',  // أغمق درجة - خلفية التطبيق الرئيسية
        },
        // zinc = درجات اللون الرمادي - تُستخدم للنصوص
        zinc: {
          50: '#fafafa',
          100: '#f4f4f5',  // لون النص الرئيسي (أبيض تقريباً)
          200: '#e4e4e7',
          300: '#d4d4d8',  // لون النص الثانوي
          400: '#a1a1aa',  // لون النص الباهت
          500: '#71717a',
          600: '#52525b',
          700: '#3f3f46',
          800: '#27272a',
          900: '#18181b',
          950: '#09090b',
        },
      },

      // ============ الخطوط المخصصة ============
      fontFamily: {
        // خط Inter كخط أساسي مع خطوط احتياطية
        sans: ['Inter', 'system-ui', '-apple-system', 'sans-serif'],
      },

      // ============ ظلال مخصصة ============
      boxShadow: {
        // ظل متوهج باللون الأزرق - يُستخدم للأزرار والبطاقات عند التمرير
        'glow-blue': '0 0 20px rgba(59, 130, 246, 0.3)',
        'glow-blue-lg': '0 0 40px rgba(59, 130, 246, 0.4)', // نسخة أكبر من التوهج
      },

      // ============ الحركات (Animations) ============
      animation: {
        'fade-in': 'fadeIn 0.3s ease-out',        // ظهور تدريجي
        'slide-up': 'slideUp 0.3s ease-out',      // انزلاق للأعلى
        'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite', // نبض بطيء
      },

      // ============ تعريف إطارات الحركة (Keyframes) ============
      keyframes: {
        // حركة الظهور التدريجي: يبدأ العنصر شفافاً ومنزلقاً للأسفل ثم يظهر في مكانه
        fadeIn: {
          '0%': { opacity: '0', transform: 'translateY(10px)' },   // البداية: شفاف ومنزلق 10px للأسفل
          '100%': { opacity: '1', transform: 'translateY(0)' },    // النهاية: مرئي وفي مكانه
        },
        // حركة الانزلاق للأعلى: مشابهة للظهور التدريجي لكن مع انزلاق أكبر
        slideUp: {
          '0%': { transform: 'translateY(20px)', opacity: '0' },
          '100%': { transform: 'translateY(0)', opacity: '1' },
        },
      },

      // ============ تمويه الخلفية ============
      backdropBlur: {
        xs: '2px', // تمويه خفيف جداً للخلفية
      },
    },
  },

  // الإضافات - لا توجد إضافات مستخدمة حالياً
  plugins: [],
}
